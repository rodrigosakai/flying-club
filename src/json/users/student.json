{
    "title": "student",
    "type": "object",
    "required": [
        "email",
        "name",
        "address",
        "date_of_birth"
    ],
    "additionalProperties": false,
    "properties": {
        "name": {
            "type": "string"
        },
        "email": {
            "type": "string",
            "format": "email"
        },
        "address": {
            "type": "string"
        },
        "date_of_birth": {
            "type": "string",
            "format": "date"
        },
        "role": {
            "type": "string",
            "enum": [
                "student"
            ]
        },
        "password": {
            "type": "string"
        }
    }
}